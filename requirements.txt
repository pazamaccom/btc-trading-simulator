# BTC Trading Simulator — Python Dependencies
# Install with: pip install -r requirements.txt

# Core
pandas>=2.0
numpy>=1.24

# Regime detection (V3 4-cluster classifier)
hmmlearn>=0.3.0
scikit-learn>=1.3
scipy>=1.11

# Data fetching
requests>=2.28

# GARCH volatility (optional — graceful fallback if missing)
arch>=6.0

# IB execution (only needed for live/paper trading, not backtesting)
ib_async>=1.0.0
nest_asyncio>=1.5.0

# Alternate data source (legacy scripts)
yfinance>=0.2.31
