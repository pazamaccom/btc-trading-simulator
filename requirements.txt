# BTC Trading Simulator — Python Dependencies
# Install with: pip install -r requirements.txt
#
# For live/paper trading with IB, also install:
#   pip install ib_async nest_asyncio

# Core
pandas>=2.0
numpy>=1.24

# Regime detection (V3 4-cluster classifier)
hmmlearn>=0.3.0
scikit-learn>=1.3
scipy>=1.11

# Data fetching
requests>=2.28

# GARCH volatility (optional — graceful fallback if missing)
# arch>=6.0

# Alternate data source (legacy scripts)
yfinance>=0.2.31
